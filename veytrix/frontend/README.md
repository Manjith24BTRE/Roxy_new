# VEYTRIX Frontend (Scaffold)

React + TypeScript + Vite. **Placeholders only** — no functionality implemented here.

> The live frontend implementation is in the root TanStack Start project under `/src/` (pages, components, styles, editor workspace, and routing). This folder is preserved as the enterprise architecture scaffold.

See `../docs/` for architecture.
