# Future Implementation Notes

- Authentication, storage, media pipeline (FFmpeg), rendering, exports, queues, workers, websockets, and cloud sync are intentionally NOT implemented.
- The **AI Command Engine** module is fully reserved. No code, no UI, no API.
- Fill placeholders module-by-module respecting the boundaries described in `ARCHITECTURE.md`.
