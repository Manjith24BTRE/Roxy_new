# Development Guide

## Setup (future)
- Install dependencies in `frontend/` and `backend/` separately.
- Follow the module boundaries — never cross feature boundaries directly.

## Rules
- Do not add business logic during scaffolding.
- Each new feature must live under its own `features/<name>/` module.
