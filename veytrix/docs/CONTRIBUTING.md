# Contribution Guide

- Branch naming: `feat/<module>/<short-desc>`
- Every feature must ship with: components, hooks, services, types, constants, utils.
- Add/maintain README.md in every module folder.
