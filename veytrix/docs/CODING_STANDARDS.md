# Coding Standards

- Strict TypeScript, no `any` in production code.
- SOLID principles.
- Feature-based modularization; no cross-feature imports.
- Presentational vs container separation.
- Explicit contracts via `interfaces/` and `types/`.
